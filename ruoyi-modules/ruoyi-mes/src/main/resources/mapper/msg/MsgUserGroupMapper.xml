<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.mes.msg.mapper.MsgUserGroupMapper">

    <select id="queryPageList" resultType="org.dromara.mes.msg.domain.vo.MsgUserGroupVo">
        select
            ug.id,
            ug.user_id,
            ug.group_id,
            g.group_name,
            g.group_code,
            u.user_code,
            u.user_name,
            ug.relative_generation_diff,
            ug.kinship_level
        from mes_msg_user_group ug
                 inner join mes_msg_user u on ug.user_id = u.id
                 inner join mes_msg_group g on ug.group_id = g.id
        <where>
            ug.deleted_flag = '0'
            and u.deleted_flag = '0'
            and g.deleted_flag = '0'
            <if test="param.id != null ">
                and ug.id = #{param.id}
            </if>
            <if test="param.userId != null ">
                and ug.user_id = #{param.userId}
            </if>
            <if test="param.groupId != null ">
                and ug.group_id = #{param.groupId}
            </if>
            <if test="param.groupName != null and param.groupName != ''">
                and (g.group_name like concat('%', #{param.groupName}, '%')
                or g.group_code like concat('%', #{param.groupName}, '%'))
            </if>
            <if test="param.userName != null and param.userName != ''">
                and (u.user_name like concat('%', #{param.userName}, '%')
                or u.user_code like concat('%', #{param.userName}, '%'))
            </if>
            <if test="param.relativeGenerationDiff != null">
                and ug.relative_generation_diff = #{param.relativeGenerationDiff}
            </if>
            <if test="param.kinshipLevel != null">
                and ug.kinship_level = #{param.kinshipLevel}
            </if>
        </where>
        order by ug.update_time desc
    </select>
</mapper>
